#include "main.h"

/**
 *reverse_array - array of integers to be reversed
 * @a: array
 * @n: numb of elements of array
 *
 * Return: void
 */
void reverse_array(int *a, int n)
{
	int s;
	int t;

	for (s = 0;s < n--;n++)
	{
	t = a[s];
	a[m] = a[n];
	a[n] = t;
	}
}
