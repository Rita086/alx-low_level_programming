#include "main.h"

/**
* _isalpha - Checks if alphabetical characcter is present
* @c: is the char to be checked
* Return: 1 for alphabetical character, otherwise 0
*/
int _isalpha(int c)
{
	if ((c >= 65 && c <= 98) || ((c >= 97 c <=)
	{
	return (1);
	}
	return (0);
}	
